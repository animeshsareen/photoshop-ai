# Public site URL (set this in production e.g. https://your-app.vercel.app)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# NextAuth Configuration (fallback if NEXT_PUBLIC_SITE_URL not set)
NEXTAUTH_URL=http://localhost:3000
# For production on Vercel, set this to your deployed URL:
# NEXTAUTH_URL=https://photoshop-ai-ten.vercel.app
# Generate a random secret: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-key-here-change-in-production

# Auth0 OAuth
# Create an Auth0 application (Regular Web App) and set Allowed Callback URLs to:
# http://localhost:3000/api/auth/callback/auth0
# https://your-production-domain/api/auth/callback/auth0
# Also set Allowed Logout URLs & Allowed Web Origins similarly.
AUTH0_CLIENT_ID=your-auth0-client-id
AUTH0_CLIENT_SECRET=your-auth0-client-secret
AUTH0_ISSUER=https://your-tenant.us.auth0.com
# Optional if calling a protected API
# AUTH0_AUDIENCE=https://your-api-identifier

# Stripe
# Get your keys from https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=sk_test_...
# Create a Product & Price for "$1 for 10 credits" and put the Price ID here (e.g., price_123)
STRIPE_PRICE_ID=price_...

# Supabase (fill with your project values)
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
# Service role is used only on the server for privileged writes
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Replicate for OpenEdit, Headshotted, Restore, and other image features
REPLICATE_API_TOKEN=
# Optional: upscaling multiplier for headshots (2-4, default 2)
# HEADSHOT_UPSCALE_INCREASE=2
